<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohgiraffers.refactorial.mail.model.dao.MailEmployeeMapper">

    <select id="searchEmployees" parameterType="String" resultType="com.ohgiraffers.refactorial.mail.model.dto.MailEmployeeDTO">
        SELECT emp_id AS empId, emp_name AS empName, dept_code AS deptCode,
        position_value AS positionValue, emp_email AS empEmail
        FROM employee
        WHERE emp_name LIKE CONCAT('%', #{empName}, '%')
    </select>

</mapper>